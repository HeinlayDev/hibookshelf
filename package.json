{
  "name": "hibookshelf",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "npm install && npm run app",
    "build": "cd hibookshelf_client && npm run build",
    "install-client": "cd hibookshelf_client && npm install",
    "heroku-postbuild": "cd hibookshelf_client && npm install --only=dev && npm install && npm run build",
    "app": "nodemon app.js",
    "client": "cd hibookshelf_client && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "proxy" : "http://localhost:8082",
  "repository": {
    "type": "git"
  },
  "author": "hein",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.19.0",
    "config": "^3.3.6",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.12.1",
    "validation": "0.0.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
